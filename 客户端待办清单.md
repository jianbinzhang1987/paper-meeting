# 无纸化会议系统客户端代办清单

## 说明

- 当前客户端主工程为 `paper-meeting-client`，技术栈为 Flutter。
- 本清单已按 `2026-04-02` 当前代码再次核对，以下“未完成事项”已补充现状、缺口和建议落点。
- 当前已接入的客户端接口只有：
  - `GET /meeting/app/bootstrap`
  - `POST /meeting/app/sign-in`
  - `GET /meeting/app/documents`
  - `GET /meeting/app/notices`
  - `GET /meeting/app/notices/page`
  - `POST /meeting/app/notices/read`
  - `GET /meeting/app/votes`
  - `POST /meeting/app/vote/submit`
  - `GET /meeting/app/realtime-state`
  - `POST /meeting/app/signature/submit`
  - `GET /meeting/app/signature/latest`
  - `GET /meeting/app/document-marks`
  - `POST /meeting/app/document-marks/save`
  - `POST /meeting/app/document-marks/delete`
  - `GET /meeting/app/service-requests`
- 当前核心业务闭环已基本形成，剩余主要是匿名表决规则收口、密码策略配置化、异常态补全和测试/真机发布流程。

## P0：优先补齐，决定客户端是否可真正上线

- [x] 文稿阅读器接入真实 PDF 引擎。
- [x] 文稿列表接口补充页数、文件类型、文件 URL、缩略图等完整字段。
- [x] 文稿页支持真实翻页、缩放、跳页，而不是仅靠本地页码变量模拟。
- [x] 文稿批注、书签实现本地持久化，至少重进应用不丢失。
- [x] 批注和书签已补后端保存与查询接口，支持跨端同步。
- [x] 签名提交改为真实上传，不再使用“发一条通知”代替提交。
- [x] 后端补充签名上传、签名记录查询接口。
- [x] 前端签名支持提交成功、失败、重复提交、清空重签等状态提示。
- [x] 视频模块已接入应用内真实播放。
- [x] 资料库模块接入真实下载/打开/预览能力，移除当前禁用按钮。
- [x] 设备配置、签到信息、最近一次会议基础信息做本地持久化。
- [x] 断网重连改为真实网络状态检测，不再只做按钮模拟。
- [x] 资料预加载改为真实下载缓存，不再只做进度条动画。

## P1：核心业务闭环，当前大多只有演示态

- [x] 秘书端“开始表决”调用后端真实接口，而不是只改本地状态。
- [x] 秘书端“结束表决”调用后端真实接口。
- [x] 秘书端“发布结果”调用后端真实接口。
- [x] 客户端表决结果刷新时展示真实票数和当前用户是否已投状态。
- [x] 匿名表决已统一规则：普通参会端在结果发布前隐藏统计，主持人/秘书可见，发布后全部终端显示汇总结果。
- [x] 广播消息改为真实发布接口或 WebSocket 下发，不再只插入本地通知列表。
- [x] 强制返回同屏改为真实服务端广播控制。
- [x] 强制停止同屏改为真实服务端广播控制。
- [x] 强制注销签到补充完整协议、服务端消息类型和客户端处理逻辑。
- [x] 客户端补齐 `FORCE_LOGOUT` 处理，收到消息后应清理会话并回到签到页。
- [x] 同屏申请、审批、拒绝、停止统一消息结构，避免前后端字段不一致。
- [x] 同屏状态要能区分“发起人”“审批人”“跟随者”“自由阅读者”。
- [x] 服务呼叫请求补充历史记录查询和状态流转记录。
- [x] 服务处理结果已补 `已接单 / 处理中 / 已完成 / 已取消` 状态定义。
- [x] 会议通知支持已读状态、未读提示和多条通知稳定分页。

## P2：可用性和体验增强

- [x] 会议信息页区分应到、已签到、在线人数，不能都直接使用用户总数。
- [x] 登录页支持按座位自动筛选账号，减少平板误选。
- [x] 账号签到已按服务端策略启用“会议密码 + 个人密码”校验，并由启动数据下发是否必填。
- [x] 文稿区的动态水印改为真实随页渲染，而不是简单覆盖文字。
- [x] 视频同屏支持播放、暂停、进度同步。
- [x] 资料库支持分类、搜索、最近访问、已下载标记。
- [x] 通知页支持自动轮播开关和置顶通知。
- [x] 会议计时器已补秘书端统一下发与断线恢复。
- [x] 实时消息日志增加筛选、级别和导出能力，便于排查联调问题。
- [x] 已补错误态页面：无会议、无文稿、无通知、接口失败、WebSocket 断开/离线浏览。
- [x] 已补基础空态与错误提示：无文稿、无通知、无表决、无资料、无服务回执、接口失败提示。

## 后端接口补充建议

- [x] 新增签名上传接口，例如 `/meeting/app/signature/submit`。
- [ ] 新增资料下载或授权访问接口，明确文件访问鉴权。
- [x] 新增广播消息发布接口，或明确由管理端发布后通过 WebSocket 下发客户端。
- [x] 新增客户端控制通道，已统一承载同屏、强制返回、强制退出、视频控制、计时控制。
- [x] 当前秘书控制已走客户端 WebSocket 通道，服务端也已按参会角色做主持人/秘书权限校验；后续只需给新增的视频控制等指令补同类校验。
- [x] 为 `bootstrap` 返回的文稿、视频、资料补齐展示所需字段，避免前端二次猜测。
- [x] 为 WebSocket 定义固定协议文档，至少包含消息类型、字段说明、示例载荷。
- [x] 为客户端提供“会议实时状态”查询接口，便于断线重连后恢复现场。

## 前端代码层面的直接问题

- [x] `DocumentPanel` 当前主阅读区仍是占位文案，需要替换为真实阅读组件。
- [x] `ApiMeetingRepository` 当前将文稿页数写死为 `1`，需要改为服务端真实字段。
- [x] `ApiMeetingRepository` 当前只把文稿 URL 拼进 `summary`，需要单独模型字段承载。
- [x] `submitSignature()` 当前只是调用 `broadcastNotice()`，需要改为真实业务提交。
- [x] `broadcastNotice()` 当前只改本地 `session.notices`，未真正发到服务端。
- [x] `forceReturnSync()` 当前只改本地状态，未发送控制消息。
- [x] `startVote()`、`finishVote()`、`publishVote()` 当前只更新本地状态。
- [x] `toggleNetworkStatus()` 当前是模拟开关，不是真实网络探测。
- [x] `simulatePreload()` 当前是模拟进度，不是真实缓存。
- [x] 客户端当前没有本地存储方案，配置和用户操作记录会丢失。

## 联调顺序建议

- [x] 第一阶段：`文稿真实预览 + 签名上传 + 资料下载 + 视频播放/受控同步` 已打通。
- [x] 第二阶段：打通 `秘书端表决控制 + 广播通知 + 强制同屏控制`。
- [x] 第三阶段：`断点恢复 + 本地缓存 + 持久化` 已基本完成，剩路由级异常页仍可继续增强。
- [x] 第四阶段：已补 `自动化测试 + 真机联调清单 + 发布流程文档`。

## 验收标准建议

- [x] 设备首次配置后，关闭应用再次进入仍可恢复服务器和座位配置。
- [x] 用户签到成功后，刷新或重进应用可恢复当前会议上下文。
- [x] 文稿可以真实打开 PDF，支持翻页、跳页、缩放。
- [x] 签名提交后服务端可查询到签名结果。
- [x] 视频可播放并支持受控同步。
- [x] 资料可下载或预览。
- [x] 秘书发起表决后，参会端能实时收到状态变化。
- [x] 秘书发布通知后，所有客户端能实时接收。
- [x] 强制返回同屏、强制停止同屏、强制注销能在所有终端生效。
- [x] 断网重连后，客户端能够恢复会议通知、服务请求、同屏状态等实时信息。
- [x] 已补基础 widget test 和 7 条控制器单测，并补齐真机联调清单与发布流程文档。

## 未完成事项拆解（2026-04-02 代码核对）

### 1. 批注与书签跨端同步

- 当前状态：已完成。
- 现状：客户端批注、书签已支持本地持久化和服务端同步，切换设备后可恢复；客户端也已支持删除。
- 对应代码：`paper-meeting-client/lib/services/local_storage_service.dart`、`paper-meeting-client/lib/state/app_controller.dart`、`paper-meeting-server/.../AppMeetingController.java`。
- 已补齐：`meeting_document_mark` 数据表，以及 `GET/POST` 查询、保存、删除接口。
- 后续可选增强：如果需要“编辑批注”“冲突合并”“离线回放”再继续细化版本字段与同步策略。

### 2. 视频模块真实播放与受控播放

- 当前状态：已完成。
- 现状：客户端视频区已接入应用内播放器，支持播放、暂停、重播、拖动进度和外部打开；秘书端的视频打开、暂停、继续、拖动也可通过 WebSocket 同步到受控终端。
- 对应代码：`paper-meeting-client/lib/screens/feature_panels.dart`、`paper-meeting-client/lib/state/app_controller.dart`、`paper-meeting-server/.../MeetingVideoControlMessageListener.java`。
- 已补齐：视频控制消息类型、实时状态快照和客户端跟播逻辑。
- 后续可选增强：若要做到更强一致性，可继续补周期性进度校准和抖动容错策略。

### 3. 匿名表决规则需要前后端统一

- 当前状态：已完成。
- 现状：后端 `/meeting/app/votes` 已按查看者角色和发布时间控制统计口径；普通参会端在匿名结果发布前只能看到“本人是否已投”，主持人/秘书可见统计，发布后所有终端显示汇总结果。
- 对应代码：`paper-meeting-server/.../AppMeetingController.java`、`paper-meeting-server/.../MeetingVoteController.java`、`paper-meeting-client/lib/repositories/api_meeting_repository.dart`、`paper-meeting-client/lib/screens/feature_panels.dart`。
- 已补齐：表决发布态持久化，刷新后不再从“已发布”退回“已结束”。
- 验收口径：匿名表决在 `进行中 / 已结束未发布 / 已发布` 三个阶段展示一致，不会在普通参会端提前泄露统计结果。

### 4. 服务呼叫要补历史记录与完整状态流转

- 当前状态：已完成。
- 现状：客户端和服务端已支持 `pending / accepted / processing / completed / canceled` 状态流转，且服务请求已落库并可按会议/用户分页查询。
- 对应代码：`paper-meeting-client/lib/models/app_models.dart`、`paper-meeting-client/lib/state/app_controller.dart`。
- 已补齐：`meeting_service_request` 数据表、分页查询接口、受理/完成/取消时间字段，以及处理结果备注字段。
- 后续可选增强：如果需要“处理轨迹时间线”可再增加独立日志表或 timeline 接口。

### 5. 会议通知已读状态与稳定分页

- 当前状态：已完成。
- 现状：客户端通知已支持服务端已读回写、未读提示、置顶、自动轮播和分页加载；同一账号换设备可恢复已读状态。
- 对应代码：`paper-meeting-client/lib/state/app_controller.dart`、`paper-meeting-client/lib/screens/feature_panels.dart`、`paper-meeting-server/.../AppMeetingController.java`。
- 已补齐：`meeting_notice_read` 数据表、`GET /meeting/app/notices/page`、`POST /meeting/app/notices/read`。
- 后续可选增强：如果通知量进一步增大，可再切换成游标分页或服务端排序策略优化。

### 6. 会议密码与个人密码要拆分

- 当前状态：已完成。
- 现状：客户端签到页已按启动数据动态显示“会议密码”和“个人密码”输入项；服务端同时校验 `meetingPassword` 与 `userPassword`，并下发每个参会人的个人密码必填策略。
- 对应代码：`paper-meeting-client/lib/models/app_models.dart`、`paper-meeting-client/lib/state/app_controller.dart`、`paper-meeting-client/lib/screens/app_shell_screen.dart`、`paper-meeting-server/.../AppMeetingController.java`。
- 已补齐：会议密码是否启用、个人密码是否必填均由服务端启动数据驱动，客户端不再只靠本地静态字段推断。
- 验收口径：仅启用会议密码时展示会议密码输入；仅启用个人密码时展示个人密码输入；两者都启用时分别独立校验。

### 7. 视频同屏的播放/暂停/进度同步

- 当前状态：已完成。
- 现状：视频控制已从普通同屏里独立出来，具备当前视频 ID、播放位置、播放状态和发送时间等状态字段；客户端收到控制消息后可跟播。
- 对应代码：`paper-meeting-client/lib/state/app_controller.dart`、`paper-meeting-client/lib/services/meeting_websocket_service.dart`、`paper-meeting-server/.../MeetingWebSocketMessageType.java`。
- 后续可选增强：进一步补更严格的时间对齐策略和多终端偏移修正。

### 8. 会议统一计时

- 当前状态：已完成。
- 现状：会议计时器已支持秘书端统一下发，参会端可跟随实时消息恢复剩余时间，断线重连后也可从实时状态接口恢复。
- 对应代码：`paper-meeting-client/lib/state/app_controller.dart` 中 `setTimer()`、`startTimer()`、`stopTimer()`。
- 已补齐：计时消息类型、实时状态快照和客户端跟随逻辑。
- 后续可选增强：若用于正式发言控制，可再补“当前发言人来源”“超时提示音”“主持端重置策略”。

### 9. 完整错误态页面

- 当前状态：已完成。
- 现状：客户端已补齐 `无会议`、`启动失败`、`接口失败可重试`、`实时连接中断`、`离线浏览切换` 等路由级状态页；模块内空态也已独立处理。
- 对应代码：`paper-meeting-client/lib/screens/feature_panels.dart`、`paper-meeting-client/lib/screens/app_shell_screen.dart`。
- 已补齐：实时断连后可直接重试连接，或在存在本地文稿/通知/缓存资源时继续离线浏览。
- 验收口径：启动失败、服务端无会议、实时断连、离线浏览四类场景都提供明确操作入口。

### 10. 自动化测试与真机联调

- 当前状态：已完成。
- 现状：当前已具备 1 条 `widget test` 和 7 条 `AppController` 单测，覆盖通知已读、同步角色、无会议、文稿标记增删、密码必填校验、离线浏览闸门等核心场景，`flutter test` 可通过。
- 已补齐：新增 [客户端联调与发布清单.md](E:/code/会议系统/客户端联调与发布清单.md)，沉淀自动化基线、真机联调步骤和发布后回归项。
- 验收口径：后续版本可按文档执行自动化、真机和发布回归，不再依赖口头传递。

## 参考定位

- 客户端入口：`paper-meeting-client/lib/app.dart`
- 客户端主界面：`paper-meeting-client/lib/screens/app_shell_screen.dart`
- 客户端功能面板：`paper-meeting-client/lib/screens/feature_panels.dart`
- 客户端状态控制：`paper-meeting-client/lib/state/app_controller.dart`
- 客户端接口封装：`paper-meeting-client/lib/repositories/api_meeting_repository.dart`
- 客户端实时通信：`paper-meeting-client/lib/services/meeting_websocket_service.dart`
- 服务端客户端接口：`paper-meeting-server/yudao-module-meeting/src/main/java/cn/iocoder/yudao/module/meeting/controller/app/AppMeetingController.java`
- 服务端实时消息类型：`paper-meeting-server/yudao-module-meeting/src/main/java/cn/iocoder/yudao/module/meeting/websocket/MeetingWebSocketMessageType.java`
