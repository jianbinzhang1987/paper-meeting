# 无纸化会议系统管理端未完成事项

## 说明

- 范围以 `paper-meeting-vue3` 管理端和 `paper-meeting-server/yudao-module-meeting` 会议后台为主。
- 判定方式以“需求文档/设计文档已有要求”对照“当前代码已落地能力”。
- 本文件只保留仍未完成或仅完成基础版的事项；本轮已补上的菜单顺序和表决控制接入不再列为未完成。

## P0：影响上线闭环，优先继续完成

- [x] 公共资料库长期运营能力已补齐一轮后端闭环。
  已新增后端分类树接口与归档策略接口（支持按规则批量归档/按资料手动归档、归档后停用、归档时间留痕），形成“录入-运营-归档”链路。
- [x] 安装包管理已补齐端侧更新运营闭环。
  已新增安装包“下发检查更新 / 重推待升级终端”接口与管理端入口，可输出终端级下发结果（已下发、失败、已命中）和失败原因（离线、连接异常等）。
- [x] 客户端样式管理已补齐推送与重推闭环。
  已新增样式“下发更新 / 重推未回执终端”接口与管理端入口，可展示终端级状态、失败原因与最近心跳，支持按未命中终端重推。

## P1：需求已写入，但管理端仍未真正落地

- [x] 会中消息发布追踪已形成管理端闭环。
  管理端已支持已读汇总、已读/未读名单明细、通知送达/失败统计和单条消息设备级回执（含终端在线状态、失败原因与最后心跳）。

## P2：可用性与权限治理仍需补齐

- [x] 会议系统权限点已完成一轮高风险动作细化。
  已新增并接入 `meeting:notification:publish`、`meeting:vote:publish`、`meeting:vote:export`、`meeting:vote:force-return` 独立权限，覆盖消息发布、结果发布、导出、强制同屏等高风险动作。
- [x] 会议首页快捷告警已完成一轮深化。
  首页在待审批、即将开始、会议室冲突基础上，新增终端异常告警（连接异常/心跳超时）并可直达终端回执页面处理。
- [ ] 管理端验收文档仍需后续补测试结果。
  本轮已新增端到端验收清单文档，但还没有填入执行结果、截图证据和缺陷回归记录。

## 本轮已完成

- [x] 重新整理会议系统菜单顺序。
  调整为“会议首页 -> 我的会议 -> 预定审批 -> 会议列表 -> 已归档会议 -> 会议模板 -> 会议室管理 -> 会中消息 -> 公共资料库 -> 客户端样式 -> 安装包管理”。
- [x] 修复会议详情相关的权限缺口。
  已在菜单 SQL 中补充 `agenda / attendee / file / vote` 相关权限节点，避免 `meeting_admin` 一类角色进入详情页后出现接口鉴权缺失。
- [x] 管理端接入表决开始/结束/发布/导出/强制返回同屏。
  复用现有后端接口，先把已具备服务端能力但前端未接上的表决控制补齐。
- [x] 独立“表决控制中心”页面已落地。
  管理端新增独立页面，支持会中会议切换、投票进度查看，以及开始/结束/发布/导出/强制返回同屏等集中操作。
- [x] 已归档会议补齐签到表和批注导出。
  已在归档页接入签到表导出，并新增批注 Excel 导出接口与前端入口。
- [x] 会中消息增加基础已读统计。
  管理端通知列表已接入已读/未读人数统计，可直接查看每条通知的阅读进度。
- [x] 会中消息补充已读/未读名单明细。
  管理端通知页可按单条消息展开阅读明细，区分已读名单和未读名单。
- [x] 公共资料库页面补齐目录树、上传和预览体验。
  管理端已支持按分类路径浏览资料、上传文件自动回填地址，并在右侧直接做图片/PDF 预览。
- [x] 安装包管理补充上传入口和版本概览。
  管理端已支持安装包上传回填、版本运营概览、MD5/下载地址校验提示，启用前可先完成基础自检。
- [x] 客户端样式页补充实时视觉预览。
  列表页与编辑弹窗均可即时预览主色、辅色、背景图、Logo、字号的终端效果。
- [x] 会议首页增加快捷告警卡片。
  首页已增加待审批会议、两小时内开始会议和会议室冲突提醒，支持直接跳转处理。
- [x] 补充管理端端到端验收清单文档。
  已新增“预约 -> 审批 -> 会前配置 -> 会中控制 -> 归档导出”的验收检查项和建议执行顺序。
- [x] 会议贴牌配置页已落地。
  管理端已新增会议贴牌管理页和后端接口，可维护网站名称、Logo、侧边栏标题与副标题。
- [x] 会议用户组能力已接入会务流程。
  管理端已新增会议用户组页面，并在参会人配置中支持按用户组批量导入参会人。
- [x] 菜单分组结构已在初始化 SQL 中落地。
  新环境执行菜单 SQL 后，会按“会议工作台 / 会中服务 / 资源配置 / 终端运维”四组生成会议菜单。
- [x] 公共资料库补充访问审计与使用追踪。
  管理端已新增资料访问日志上报与统计，能记录浏览、打开、复制链接等访问痕迹。
- [x] 管理端接入终端心跳与配置回执视图。
  后端已新增统一终端状态表与客户端心跳接口，安装包管理和客户端样式页都可看到在线终端、命中数量、终端明细和最近心跳时间。
- [x] 会中消息补齐终端送达/失败回执追踪。
  管理端通知阅读明细弹窗新增设备级送达回执表，支持送达/失败/待送达统计、失败原因、终端在线状态与最后心跳时间。
- [x] 会议首页补充终端异常快捷告警。
  首页快速告警新增“终端异常”卡片，可展示连接异常或心跳超时终端并跳转到样式页终端回执明细。
- [x] 高风险会中动作改为独立权限控制。
  后端接口鉴权与前端按钮校验已同步改为独立权限，不再复用笼统的 `update` 权限。
- [x] 安装包与样式补齐下发/重推结果闭环。
  新增终端下发接口与前端操作入口，可对目标配置执行“全量下发”或“仅未命中重推”，并展示终端级结果与失败原因。
- [x] 公共资料库补齐长期归档策略。
  新增公共资料归档能力，支持按时间规则批量归档、按单条资料归档，并维护归档目录层级和归档操作留痕。

## 本轮验证记录（2026-04-02）

- [x] 后端会议模块编译通过。
  已执行 `mvn -pl yudao-module-meeting -am compile -Dmaven.test.skip=true`，结果为 `BUILD SUCCESS`。
- [x] P0 三项闭环已完成代码与页面联动。
  公共资料归档、安装包下发/重推、样式下发/重推均已覆盖“后端接口 + 前端入口 + 终端级结果明细”。
- [ ] 管理端验收清单待补执行证据。
  需在 `系统文档/管理端验收清单.md` 填写本轮执行结果、截图与回归记录。
